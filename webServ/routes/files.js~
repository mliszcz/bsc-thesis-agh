var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/files', function(req, res) {
  res.render('files', { records: 'Express' });
});

module.exports = router;
